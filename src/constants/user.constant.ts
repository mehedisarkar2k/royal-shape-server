export enum UserType {
  ADMIN = "admin",
  CUSTOMER = "customer"
}
export const UserTypeValues = Object.values(UserType);

export enum UserRole {
  ADMIN = "admin",
  MODERATOR = "moderator",
  EMPLOYEE = "employee",
  CUSTOMER = "customer"
}
