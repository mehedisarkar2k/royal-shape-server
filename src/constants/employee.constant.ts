export enum EmploymentStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
  SUSPENDED = "suspended",
  ON_LEAVE = "on_leave"
}

export enum EmploymentType {
  FULL_TIME = "full_time",
  PART_TIME = "part_time",
  CONTRACT = "contract",
  INTERN = "intern"
}

export enum JobAdStatus {
  ACTIVE = "active",
  DRAFT = "draft",
  CLOSED = "closed"
}
